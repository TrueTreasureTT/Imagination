# Imagination

Node.js Scramjet-powered web application.

## Run

```bash
npm install
npm start
```

Open http://localhost:8080. Production deployments need HTTPS because browsers require a secure context for service workers.